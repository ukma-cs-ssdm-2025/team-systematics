# Workflow: CI Test
#
# Призначення:
#   Простий тестовий workflow для перевірки базової роботи GitHub Actions.
#   ⚠️ Цей workflow містить лише тестовий код і може бути видалений або замінений.
#
# Тригери:
#   - Push до будь-якої гілки
#
# Що робить:
#   Виводить "Hello, World!" для перевірки роботи CI системи.
#
# Статус:
#   Застарілий workflow - використовується run-api-test.yml для реального тестування

name: CI Test
on: [push]
jobs:
   build:
     runs-on: ubuntu-latest
     steps:
     - uses: actions/checkout@v3
     - name: Run Hello World
       run: echo "Hello, World!"