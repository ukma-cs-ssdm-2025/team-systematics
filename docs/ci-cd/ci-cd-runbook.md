# CI/CD Runbook - GitHub Actions Workflows

## Огляд

Цей документ описує всі GitHub Actions workflows, що використовуються в проєкті, їх призначення, тригери та процеси виконання.

## 📋 Зміст

- [Огляд Workflows](#огляд-workflows)
- [Детальний опис кожного Workflow](#детальний-опис-кожного-workflow)
- [Troubleshooting](#troubleshooting)
- [Моніторинг та сповіщення](#моніторинг-та-сповіщення)

---

## Огляд Workflows

| Workflow | Призначення | Тригери | Статус |
|----------|-------------|---------|--------|
| **run-api-test.yml** | Запуск автоматичних тестів API<br/>• `test-unit`: швидкі тести без ML<br/>• `test-ml`: тести з ML (тільки на main) | Push, PR до main | ✅ Активний |
| **generate-docs.yml** | Генерація OpenAPI документації<br/>(використовує requirements-base.txt) | Push до main | ✅ Активний |
| **deploy-docs.yml** | Деплой документації на GitHub Pages | Push до main (тільки docs/api/) | ✅ Активний |
| **sonarcloud.yml** | Статичний аналіз коду | Push, PR до main | ✅ Активний |
| **uml-render.yml** | Генерація UML діаграм | Push до тестових гілок | ⚠️ Обмежений |
| **ci-test.yml** | Тестовий workflow | Push | ⚠️ Застарілий |

---

## Детальний опис кожного Workflow

### 1. Run API Tests (`run-api-test.yml`)

**Призначення:** Автоматичне тестування API бекенду при кожному зміні коду. Використовує оптимізовану стратегію з розділенням на швидкі unit тести та повільніші ML тести.

**Тригери:**
- Push до гілки `main`
- Pull Request до гілки `main`
- Push до тестової гілки `allyxandraaa/fix-test-workflow`

**Структура Jobs:**

#### Job 1: `test-unit` - Швидкі Unit Тести (без ML)

**Призначення:** Швидкі тести без ML бібліотек для швидкої перевірки коду.

**Процес виконання:**
1. Checkout коду з репозиторію
2. Встановлення Python 3.11 з кешуванням pip залежностей
3. Встановлення базових залежностей з `requirements-base.txt` (без ML бібліотек)
4. Встановлення тестових залежностей з `pyproject.toml`
5. Запуск тестів через `pytest`

**Змінні середовища:**
- `PYTHONPATH`: дозволяє імпортувати модулі з `src/`
- `DATABASE_URL`: `sqlite:///:memory:` (in-memory БД для ізоляції тестів)
- `JWT_SECRET`: `test-secret` (тестовий секрет)

**Особливості:**
- Використовує fallback на TF-IDF (через scikit-learn) замість ML моделей
- Швидше виконання (~30-60 секунд замість 2+ хвилин)
- Запускається завжди при push/PR

**Критичність:** 🔴 Високо критичний - блокує мердж PR якщо тести не проходять

#### Job 2: `test-ml` - Integration Тести (з ML)

**Призначення:** Повні тести з ML бібліотеками для перевірки ML функціональності.

**Процес виконання:**
1. Checkout коду
2. Встановлення Python 3.11 з кешуванням pip
3. Встановлення базових залежностей з `requirements-base.txt`
4. Встановлення ML залежностей з `requirements-ml.txt` (torch, transformers, sentence-transformers)
5. Встановлення тестових залежностей
6. Запуск тестів через `pytest`

**Змінні середовища:**
- `PYTHONPATH`: дозволяє імпортувати модулі з `src/`
- `DATABASE_URL`: `sqlite:///:memory:`
- `JWT_SECRET`: `test-secret`

**Особливості:**
- Запускається тільки на push до `main` (не на PR)
- Виконується паралельно з `test-unit`
- Повільніше через встановлення ML бібліотек (~2-3 хвилини)
- Перевіряє ML функціональність (семантична схожість через sentence-transformers)

**Критичність:** 🟡 Середня - перевіряє ML функціональність, але не блокує PR

**Оптимізація:**
- Розділення на два job'и дозволяє швидко отримати feedback від unit тестів
- ML тести запускаються рідше (тільки на main) для економії часу CI/CD

---

### 2. Generate API Docs (`generate-docs.yml`)

**Призначення:** Автоматична генерація OpenAPI специфікації з FastAPI додатку та оновлення документації.

**Тригери:**
- Push до гілки `main`
- ⚠️ Не запускається для комітів від `github-actions[bot]` (запобігає циклічним оновленням)

**Процес виконання:**
1. Checkout коду
2. Встановлення Python 3.11 з кешуванням pip залежностей
3. Встановлення базових залежностей з `requirements-base.txt` (без ML бібліотек)
4. Генерація OpenAPI YAML через `python -m src.api.export-openapi`
5. Створення Pull Request з оновленнями
6. Автоматичний merge PR (squash merge)

**Змінні середовища:**
- `DATABASE_URL`: `sqlite:///:memory:` (не потрібна реальна БД)
- `JWT_SECRET`: `supersecretkey` (дефолтне значення з `config.py`)
- `JWT_ALGORITHM`: `HS256`
- `JWT_EXPIRATION_MINUTES`: `60`

**Оптимізація:**
- Використовує тільки `requirements-base.txt` (без ML бібліотек) для швидшої генерації документації
- ML бібліотеки не потрібні для генерації OpenAPI специфікації

**Критичність:** 🟡 Середня - документація має бути актуальною, але не блокує розробку

**Особливості:**
- Автоматично створює PR з міткою `automerge` та `documentation`
- PR автоматично мерджиться після проходження checks
- Гілка `openapi-auto-update` видаляється після мерджу

---

### 3. Deploy Docs to GitHub Pages (`deploy-docs.yml`)

**Призначення:** Публікація API документації на GitHub Pages для публічного доступу.

**Тригери:**
- Push до гілки `main` або `allyxandraaa/configure-github-pages-workflows`
- ⚠️ Тільки якщо змінені файли в `docs/api/**`

**Процес виконання:**
1. Checkout коду
2. Завантаження вмісту `docs/api/` як artifact
3. Деплой на GitHub Pages через вбудований deployment action

**Критичність:** 🟢 Низька - документація доступна локально, Pages - додатково

**Результат:**
- Документація доступна на GitHub Pages URL (вказується в environment `github-pages`)

---

### 4. SonarCloud Analysis (`sonarcloud.yml`)

**Призначення:** Статичний аналіз коду для виявлення вразливостей та проблем якості.

**Тригери:**
- Push до гілки `main`
- Pull Request до гілки `main`
- Manual trigger через `workflow_dispatch`

**Процес виконання:**
1. Аналіз коду через SonarCloud
2. Створення Code Scanning alerts для знайдених проблем
3. Декорування PR з результатами аналізу

**Потрібні налаштування:**
- `SONAR_TOKEN` в GitHub Secrets
- `sonar.projectKey` та `sonar.organization` в workflow файлі

**Критичність:** 🟡 Середня - покращує якість коду, але не блокує розробку

---

### 5. Generate PlantUML Diagrams (`uml-render.yml`)

**Призначення:** Генерація SVG діаграм з PlantUML файлів для документації архітектури.

**Тригери:**
- Push до гілок:
  - `allyxandraaa/add-uml-diagrams`
  - `allyxandraaa/change-generate-puml-workflow`

**Процес виконання:**
1. Checkout коду
2. Генерація SVG з `.puml` файлів через PlantUML
3. Збереження результатів в `docs/architecture/uml/uml-svgs/`

**Критичність:** 🟢 Низька - використовується для документації

**⚠️ Обмеження:** Запускається тільки на тестових гілках. Для автоматизації на main потрібно додати гілку до тригерів.

---

### 6. CI Test (`ci-test.yml`)

**Призначення:** Простий тестовий workflow для перевірки базової роботи GitHub Actions.

**Тригери:**
- Push до будь-якої гілки

**Процес виконання:**
- Виводить "Hello, World!"

**Статус:** ⚠️ **Застарілий** - містить лише тестовий код. Може бути видалений або замінений.

**Рекомендація:** Використовувати `run-api-test.yml` для реального тестування.

---

## Troubleshooting

### Проблема: Unit тести падають без зрозумілих причин

**Симптоми:**
- Job `test-unit` падає з помилками підключення до БД
- Помилки імпорту модулів
- Помилки про відсутні ML бібліотеки

**Рішення:**
1. Перевірити, що `DATABASE_URL` встановлено як `sqlite:///:memory:`
2. Перевірити, що `PYTHONPATH` встановлено як `${{ github.workspace }}`
3. Перевірити, що встановлюються тільки базові залежності: `pip install -r requirements-base.txt`
4. Переконатися, що код використовує fallback на TF-IDF (не вимагає ML бібліотек)
5. Перевірити лог workflow для деталей помилки

---

### Проблема: ML тести не запускаються

**Симптоми:**
- Job `test-ml` не з'являється в списку виконаних jobs
- ML тести не виконуються на PR

**Рішення:**
1. Перевірити умову запуску: `test-ml` запускається тільки на push до `main`
2. На PR ML тести не запускаються навмисно (для економії часу)
3. Для тестування ML функціональності на PR можна тимчасово змінити умову в workflow:
   ```yaml
   if: github.event_name == 'pull_request' || (github.event_name == 'push' && github.ref == 'refs/heads/main')
   ```

---

### Проблема: ML тести падають при встановленні залежностей

**Симптоми:**
- Помилки під час встановлення torch/transformers
- Timeout при встановленні ML бібліотек

**Рішення:**
1. Перевірити, що встановлюються обидва файли:
   ```bash
   pip install -r requirements-base.txt
   pip install -r requirements-ml.txt
   ```
2. ML бібліотеки великі - встановлення може тривати 2-3 хвилини (це нормально)
3. Перевірити кешування pip - воно має прискорити повторні встановлення
4. Якщо проблема повторюється, можна тимчасово пропустити ML тести (вони не блокують PR)

---

### Проблема: Generate Docs workflow падає

**Симптоми:**
- Помилки під час імпорту app
- Помилки підключення до БД
- Помилки про відсутні залежності

**Рішення:**
1. Перевірити, що всі змінні середовища встановлені:
   - `DATABASE_URL: "sqlite:///:memory:"`
   - `JWT_SECRET: "supersecretkey"`
2. Перевірити, що встановлюються базові залежності: `pip install -r requirements-base.txt`
3. ML бібліотеки не потрібні для генерації документації
4. Перевірити, що `src.api.export-openapi` може працювати без реальної БД
5. Перевірити лог workflow для деталей помилки

---

### Проблема: Тести працюють локально, але падають в CI

**Симптоми:**
- Тести проходять локально з `requirements.txt`
- Тести падають в CI з `requirements-base.txt`

**Рішення:**
1. Перевірити, що локально використовується той самий набір залежностей
2. Перевірити, що код не залежить від ML бібліотек (використовує fallback)
3. Запустити тести локально з `requirements-base.txt`:
   ```bash
   pip install -r requirements-base.txt
   pip install -e ".[test]"
   pytest
   ```
4. Перевірити, що всі необхідні залежності присутні в `requirements-base.txt`

---

## Моніторинг та сповіщення

### Перевірка статусу workflows

1. **В GitHub UI:**
   - Перейти до вкладки "Actions"
   - Переглянути історію виконання workflows
   - Перевірити статус останніх запусків
   - Для `run-api-test.yml` перевірити обидва job'и: `test-unit` та `test-ml`

2. **В Pull Request:**
   - Перевірити секцію "Checks" в PR
   - `test-unit` має бути зеленим перед мерджем (критичний)
   - `test-ml` може бути відсутнім на PR (це нормально - запускається тільки на main)

3. **Badges в README:**
   - Додати workflow status badges для візуального моніторингу
   - Приклад: `[![CI Test](https://github.com/.../actions/workflows/run-api-test.yml/badge.svg)]`

### Сповіщення

**Налаштування email сповіщень:**
1. GitHub автоматично надсилає email при падінні workflows
2. Налаштування: Settings > Notifications > Actions

**Рекомендації:**
- Перевіряти вкладку Actions перед мерджем PR
- Звертати увагу на статус `test-unit` (критичний для PR)
- `test-ml` може падати без блокування PR (не критичний)
- Додати коментар у PR template про перевірку статусу workflows
- Налаштувати required status checks для гілки `main` (Settings > Branches)

---

## Best Practices

1. **Не мерджити PR якщо unit тести не проходять**
   - Завжди перевіряти статус `test-unit` перед мерджем
   - Це критичний check, який блокує мердж

2. **Розуміти різницю між unit та ML тестами**
   - Unit тести (`test-unit`) - швидкі, запускаються завжди, блокують PR
   - ML тести (`test-ml`) - повільніші, тільки на main, не блокують PR

3. **Оновлювати документацію разом з кодом**
   - OpenAPI специфікація оновлюється автоматично
   - Перевіряти, що зміни коректні

4. **Використовувати тестові гілки для експериментів**
   - Не змінювати workflows без тестування
   - Використовувати feature branches для нових workflows

5. **Документувати зміни**
   - Додавати коментарі до нових workflows
   - Оновлювати цей runbook при змінах

---

## Контакти

При питаннях або проблемах з workflows звертайтесь до:
- **Owner:** Олександра Малій (@allyxandraaa)
- **GitHub Issues:** Створіть issue з міткою `ci-cd` або `workflow`

---

*Останнє оновлення: 2025*


